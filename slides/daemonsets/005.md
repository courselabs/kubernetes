---
layout: center
---

# Rapid DaemonSet Creation

```mermaid
graph LR
    DEP[<carbon-terminal/> kubectl create<br/>deployment] --> DRY[<carbon-document/> --dry-run<br/>YAML] --> CHANGE[<carbon-edit/> Change kind<br/>Remove replicas] --> APPLY[<carbon-checkmark/> Apply]
    style DEP fill:#60a5fa
    style DRY fill:#a78bfa
    style CHANGE fill:#fbbf24
    style APPLY fill:#4ade80
```

<div class="mt-8 text-center">

## No `kubectl create daemonset` command

### Convert from Deployment manifest:
1. Generate Deployment with `--dry-run=client -o yaml`
2. Change `kind: Deployment` to `kind: DaemonSet`
3. Remove `replicas` field
4. Apply

<div class="mt-4 opacity-80">
Faster than writing from scratch
</div>

</div>
