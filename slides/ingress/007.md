---
layout: center
---

# Backend Service Configuration

```mermaid
graph TB
    PATH[<carbon-folder/> Path: /api<br/>pathType: Prefix] --> BACKEND[<carbon-network-3/> backend]

    BACKEND --> SERVICE[service.name: api-service]
    BACKEND --> PORT[service.port.number: 8080]

    style PATH fill:#60a5fa
    style BACKEND fill:#a78bfa
    style SERVICE fill:#4ade80
    style PORT fill:#4ade80
```

<div class="mt-8 text-center">

### Critical Syntax (networking.k8s.io/v1)

```yaml
backend:
  service:
    name: api-service
    port:
      number: 8080
```

<div class="mt-4 opacity-80 text-sm">
Can also use port.name instead of port.number
</div>

</div>
