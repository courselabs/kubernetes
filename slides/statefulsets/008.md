---
layout: center
---

# Conversion from Deployment

```mermaid
graph LR
    DEP[<carbon-deploy/> Deployment<br/>YAML] --> CHANGE[<carbon-edit/> Modifications] --> SS[<carbon-data-base/> StatefulSet<br/>YAML]

    style DEP fill:#60a5fa
    style CHANGE fill:#fbbf24
    style SS fill:#4ade80
```

<div class="mt-8 text-center">

### Key Changes
1. Change `kind: Deployment` → `kind: StatefulSet`
2. Add `serviceName: <headless-service>`
3. Add `volumeClaimTemplates` (if storage needed)
4. Keep `replicas`, `selector`, `template` unchanged

<div class="mt-4 opacity-80">
Most fields are identical
</div>

</div>
