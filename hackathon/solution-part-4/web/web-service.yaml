apiVersion: v1
kind: Service
metadata:
  name: widgetario-web-internal
  labels:
    co.courselabs.k8sfun: hackathon
    app: widgetario
spec:
  ports:
    - name: http
      port: 80
      targetPort: http
  selector:  
    app: widgetario
    component: web
  type: ClusterIP