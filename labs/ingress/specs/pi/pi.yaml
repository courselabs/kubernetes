apiVersion: v1
kind: Service
metadata:
  name: pi-internal
  labels:
    co.courselabs.k8sfun: ingress
spec:
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: pi-web
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pi-web
  labels:
    co.courselabs.k8sfun: ingress
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pi-web
  template:
    metadata:
      labels:
        app: pi-web
    spec:
      containers:
        - image: sixeyed/pi:2002
          command: ["dotnet", "Pi.Web.dll", "-m", "web"]
          name: pi-web
          ports:
            - name: http
              containerPort: 80
