apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: podviewer
  labels:
    co.courselabs.k8sfun: rbac
rules:
- apiGroups: [""]
  resources: ["pods", "pods/log"]
  verbs: ["get", "list"]